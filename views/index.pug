extends layout

block content
  if(user) 
    h1 Welcome back, #{user.username}
    a(href="/logout").sty-link Logout 
  else
    .hero
      h1 Chatty 
      .hero-links
        a(href="/signup").sty-link Sign up 
        a(href="/login").sty-link Login
